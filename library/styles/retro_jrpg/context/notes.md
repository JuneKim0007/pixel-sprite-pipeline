# Notes on this look

Prose the LLM orchestrator reads alongside the vocabulary. Say what the
look is *for* and what it must never do — those are the things a list of
prompt fragments cannot express.
